#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."
echo ""

# Run lint-staged (lints en test alleen gewijzigde bestanden)
npx lint-staged

echo ""
echo "✅ Pre-commit checks passed!"

